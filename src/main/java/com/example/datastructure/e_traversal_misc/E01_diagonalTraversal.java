package com.example.datastructure.e_traversal_misc;

/**
 * *****************************************************************************
 * Diagonal Traversal of Binary Tree
 * *****************************************************************************
 */

public class E01_diagonalTraversal {
	/*
	 * //take a Queue
	 * //add root to it
	 * 
	 * while (Queue is not empty) {
	 *    Node node = Queue.poll()		//dequeue the first element of queue
	 *    while (node != null) {
	 *       //print its key
	 *       if it has any left child, enqueue that child in Queue
	 *       node = node.right;
	 *    }
	 * }
	 * 
	 */
}
