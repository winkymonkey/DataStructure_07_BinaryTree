package com.example.datastructure.f_checking;

/**
 * *****************************************************************************
 * Check if all leaves are at same level
 * *****************************************************************************
 */
public class A07_check_ifAllLeavesAreAtSameLevel {
	/*
	 * int storedLevel = 0;
	 * 1.start traversing from root & recursively search in left and right subtree.
	 * 2.during traversal, pass a variable called "level" in recursion call.
	 * 3.for each level of the tree, increment level by 1.
	 * 4.if a leaf is found, compare its value with storedLevel
	 * 5.if (level!=storedLevel), break and return...otherwise continue
	 * 
	 */
}
