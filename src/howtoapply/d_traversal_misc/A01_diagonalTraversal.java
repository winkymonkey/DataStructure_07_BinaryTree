package howtoapply.d_traversal_misc;

/**
 * *****************************************************************************
 * Diagonal Traversal of Binary Tree
 * *****************************************************************************
 */
public class A01_diagonalTraversal {
	/*
	 * //take a Queue
	 * //add root to it
	 * while(Queue is not empty) {
	 * 		//dequeue the first element of queue	//Node node = Queue.poll()
	 * 		while(node != null) {
	 * 			//print its key
	 * 			//if it has any left child, enqueue that child in Queue
	 * 			//node = node.right;
	 * 		}
	 * }
	 */
}
