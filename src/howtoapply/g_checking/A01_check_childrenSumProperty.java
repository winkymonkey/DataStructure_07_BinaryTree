package howtoapply.g_checking;

/**
 * *****************************************************************************
 * Check for Children Sum Property in a Binary Tree
 * *****************************************************************************
 * For every node, data value must be equal to 
 * sum of data values in left and right children.
 * 
 *          15 
 *        /    \
 *       7      8
 *      / \    / \
 *     4   3  6   2
 *     
 * *****************************************************************************
 */
public class A01_check_childrenSumProperty {
	/*
	 * Traverse the given binary tree.
	 * For each node check (recursively) if the node and both its children satisfy the Children Sum Property.
	 * if so then return true else return false.
	 */
}
